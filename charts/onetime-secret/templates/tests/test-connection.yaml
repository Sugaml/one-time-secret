apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onetime-secret.fullname" . }}-test-connection"
  labels:
    {{- include "onetime-secret.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onetime-secret.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
